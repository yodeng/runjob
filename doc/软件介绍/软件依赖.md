#### 软件依赖

`runjob`使用`python`语言编写，需要`python >= 3.7`。

依赖的第三方`python`模块包有：

+ psutil
+ prettytable
+ rich-argparse（可选，提供彩色帮助信息）

可选功能依赖（按需安装）：

+ pytz + apscheduler（定时任务调度）
+ pyx（DAG 图可视化导出）

使用`pip install`进行安装时，会自动下载和安装相关依赖包。

